[package]
name = "jcode-plan"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
jcode-attempt-types = { path = "../jcode-attempt-types" }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
