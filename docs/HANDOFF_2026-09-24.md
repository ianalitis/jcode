# Handoff — 2026-09-24

Branch `jcode/ci-format-baseline` (the fork's integration line). Clean tree, no
write lease held. The session's own commits are listed below, but the remote
branch `fork/jcode/ci-format-baseline` is still at `d027491f6` (2026-09-20), so
**170 first-parent commits (2,174 total, 349 ahead of `origin/master`) are
unpushed**, not just these. Verified with `git ls-remote fork` on 2026-09-24.

```
d7fdf2961 docs: record the usage-window fix
faca9021d openrouter-runtime: fail fast when a usage window is spent, not after 8 retries
7edcb1261 docs: record the routing fix and what is still open
9358addba provider: stop an active compatible profile from swallowing first-party model ids
53f013b9d docs: hand off Opus 5.5, the dotfile gate, and what is still open
6d67db559 command-risk: protect shell init files and the secret stores outside ~/.ssh
c101a81d2 anthropic: add Opus 5.5 and advertise the version it requires
28db281cb command-risk: gate relocation verbs, which were not classified at all (H3)
b65dcc11c attempt-types: derive outbound eligibility from the effective class (W4)
```

Pushing is separately gated and the operator has not approved it for these.

## What landed this session

### Opus 5.5 (`c101a81d2`)

Reported symptom: a 400 on every Opus 5.5 request, saying Claude Code 2.1.257
is too old and to run `claude update`. The operator had already updated via
homebrew and the error persisted.

The advice in that message cannot work. jcode does not shell out to the Claude
Code CLI, it advertises its own hardcoded version string. Two independent
defects, both required:

- Three attribution constants pinned at `2.1.257`, bumped to `2.1.281` (the
  version actually installed, not the `2.1.280` floor named in the error).
- `claude-opus-5-5` was not in `AVAILABLE_MODELS` at all.

Model id, pricing and context window were read out of the shipping 2.1.281
binary rather than guessed. It carries the literal id and release notes stating
1M context and $4/$20 per Mtok with $0.20 cache reads. Also corrected
`cch=33f85` to `cch=00000`, which is what the 2.1.281 attribution builder emits
for first-party subscriber requests.

Verified live, not just compiled:

```
jcode run --model claude-oauth:claude-opus-5-5 'Reply with exactly: OPUS55 OK'
  -> OPUS55 OK
```

Capability tables needed no change and that was checked rather than assumed:
the version parser reads `5-5` as (5,5), giving Native1M, 128K output and the
full effort ladder.

### Dotfile and secret-store protection (`6d67db559`)

Probing after H3 found protected *directories* guarded while the files that
make them worth guarding were not. `rm ~/.bashrc`, `rm -rf ~/.local/share/keyrings`
and `rm ~/.netrc` were all **Low** (silent), while `rm -rf ~/.config` was already
Catastrophic.

Two tiers, because the losses are not equivalent: secret stores are
Catastrophic (unrecoverable, re-issuing every key), shell init files are
Confirm (recoverable work, but silently reconfigures every future shell).
Redirects and `truncate` route through `classify_target`, so
`echo '' > ~/.zshrc` is covered without touching the verb tables.

91 crate tests pass, `jcode-base` 1558 pass, provider crates 134 pass.

### First-party model routing (`9358addba`)

Fixes the item this handoff previously listed as open. `--model claude-opus-5-5`
with no `--provider` flag reached OpenCode Go and failed with
`claude-opus-5-5 is not a valid model ID`, a message that names the model when
the fault was the routing.

Two causes, found by tracing rather than reading: `ALL_CLAUDE_MODELS` had
drifted from `AVAILABLE_MODELS` (selectable, then misrouted), and the
provider-local short-circuit in `set_model` fired before inspecting the model
at all. Narrowed by exact first-party catalog membership, which preserves the
rule's real purpose for opaque ids like `claude-opus4.6-thinking`. A drift test
now prevents the two catalogs separating silently again.

Verified live: bare `claude-opus-5-5`, `claude-opus-5` and `claude-fable-5-1`
all answer from Anthropic. 191 provider tests pass.

### Spent usage windows fail fast (`faca9021d`)

Fixes the item this handoff previously listed as open item 1.
`jcode run --model deepseek-v4.1-flash` produced no output, no error, then
timed out. The provider was never slow: called directly it answers in 0.23s
with `429 GoUsageLimitError, "limitName":"weekly"`.

`is_retryable_error` treated every 429 as retryable, so with `max_retries = 8`
and a 30s backoff cap jcode spent minutes retrying a weekly allowance that
cannot reset inside any retry budget. The fix distinguishes the *window*: short
limits still retry, daily/weekly/monthly ones fail immediately. The hint is
corrected too, since "check network connectivity, DNS/TLS" is actively
misleading when a quota is spent.

Measured: a 150s+ silent hang becomes a 4.4s actionable error naming the
window. 187 crate tests pass.

## Open items, in priority order

1. **Your OpenCode Go weekly allowance is currently spent.** Not a defect, but
   it is why `deepseek-v4.1-flash` (the configured `default_model`) cannot run
   right now. jcode now says so in 4s instead of hanging. Until the window
   resets, prefer another route.

2. **Push authorization.** The fork's copy of the integration line last moved on
   2026-09-20. Pushing now publishes every first-party commit since then
   (170 on the first-parent line), not only this session's nine. Review that
   range before approving: `git log --first-parent fork/jcode/ci-format-baseline..HEAD`.

3. **Panic-budget scanner mis-counts** `build.rs` and `#[cfg(test)]` bodies.
   Named in an earlier commit as the durable follow-up rather than bundled.

## Known pre-existing failures, not caused by this work

- Code-size ratchet fails on a concurrent session's files (`jcode-base/src/jev.rs`,
  `navigation.rs`, `ui.rs`).
- `jcode-setup-hints` has one test failure that also fails on `fork/master`.
- `jcode-app-core` has ~20 process-global-state flakes under parallel runs (the
  #592 class) that pass in isolation.
- `jcode-base`'s `automatic_recall_uses_jev_http_without_embeddings_or_sidecar`
  flakes about 1 run in 6 under the full parallel suite and always passes in
  isolation. Measured at the same rate on unmodified HEAD, so it is the same
  class: a real TCP listener and a 10s deadline under parallel load.

## Working agreements to carry forward

- Explicit approval before pushing, and before any change to what a gate means.
- Fix at the source, never by loosening a gate. Raising a baseline is acceptable
  only when the baseline is demonstrably stale, and the commit must say what it
  absorbs.
- Measure, don't assume. Every claim in a commit message should be something
  actually run. Both fixes this session came from reading the shipping binary
  and probing the classifier, not from guessing.
- Don't let a safety gate become noise. Over-firing trains users to bypass it,
  which is why the routine-work test matters more than new coverage. It caught
  a wrong assertion again this session.
