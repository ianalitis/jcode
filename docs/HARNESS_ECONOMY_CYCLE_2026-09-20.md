# Bounded harness economy cycle

2026-09-20. Applies to the current source-closeout task, not unattended authority or host configuration changes.

## Objective and current evidence

Optimize accepted work per actual dollar and total elapsed/review time, subject to unchanged correctness, privacy and effect boundaries. Token price alone is not the objective. OpenAI product credits, OpenRouter invoices and API-equivalent estimates remain separate accounting categories.

Current cheap-route evidence is three closed treatments with zero accepted artifacts: unclassified parse failure, 6/12 complete boundary cases, and a 90-second timeout. Two $0.01 reservations remain ambiguous. Cost per accepted result is undefined with zero acceptances, not zero. This small, heterogeneous sample establishes neither a model ranking nor useful throughput.

## Highest-return sequence

1. Close the bounded J5 fixture investigation at its current checkpoint. Do not spend further premium context on an expanding mock/production routing redesign. Preserve unaccepted changes and exact blockers.
2. Prove and fix one shared frozen-attempt deadline across provider opening and stream consumption. Reuse `run_frozen_attempt`, its receipts and loopback fixtures. Predicted effect: delayed setup cannot grant a second full timeout window. No new retries, fallback, router, ledger or dispatcher.
3. Once request eligibility, receipt binding and accounting permit real dispatch, compare a compact no-tool proposal packet with the existing baseline on the **same task and acceptance tests**. One factor changes per treatment. Public/synthetic bytes only unless explicitly admitted otherwise. Select a paid cheap candidate only after current endpoint/privacy/price and numeric budget admission, not by substituting into a failed treatment.
4. Reduce context and tool surface for the task classes that pass. Prefer exact function/test/diagnostic slices over parent transcript inheritance. Measure serialized request bytes and provider-reported tokens separately. Do not remove mandatory safety instructions or compress executable contracts.

## Acceptance measurements

| Dimension | Evidence and rule |
| --- | --- |
| Accuracy | Captain-owned executable acceptance result, first-pass and final acceptance counts. A refusal, transport failure and incorrect result remain separate outcomes. A passing parser is not a passing task. |
| Cost | Authoritative per-attempt actual billing plus unresolved reserved exposure. Aggregate balance changes and token-price estimates are not invoices. Report unknown explicitly. Include failed attempts and corrections in totals. |
| Time | Monotonic whole-attempt elapsed time, generation/open/stream times when actually instrumented, validation time and captain review time. Do not invent phase timings from one total. |
| Networking | Request count, timeout/cancellation behavior, retry/fallback/redirect count, response identity and bytes. Loopback tests must prove at-most-one send and retained ambiguous exposure after uncertain transport outcomes. |
| Agent/context economy | Spawn count, model turns, serialized input bytes, reported input/output/cache tokens where available, tool-result bytes, and reviewer interventions. Unknown cache or credit data stays unknown. |

Compare accepted-task cost only when the denominator is nonzero. Keep queue time and active reviewer time distinct. Use paired task IDs, source/request hashes, exact route/effort and acceptance commands. Small samples are observations, not reliable p95 estimates or proof of general model superiority.

## Automatic assessment, bounded improvement

Use the existing project test gateway and attempt receipts. Do not add a second task runner or parallel state store. The first automatic assessment is a permanent regression test for the selected deadline contract, exercised with the existing caller tests. Capture the failing baseline and passing candidate on the same fixture.

Each later optimization is one explicit packet: hypothesis, fixed treatment and limits, readable/writable paths, predicted effect, acceptance command, before/after evidence and stop condition. A failed acceptance or unexpected scope closes that packet for captain review. Never silently retry on another provider, increase spend limits or enable tools.

Automatic promotion is not implemented or authorized here. A future evaluator may recommend the cheapest admissible treatment that passes paired correctness and time/cost thresholds, but missing billing, an unbound receipt, privacy failure or regressed correctness must veto promotion. Host routing changes and binary activation retain their separate approval gates.

Stop spending on a category when its bounded packet yields no accepted improvement. Resume for a demonstrated task need or a materially changed candidate, not to consume quota. Keep one writer and reuse warm isolated compilation caches without reusing unrelated conversation history.

## Execution checkpoint, 01:59 UTC

J5 reproduced under isolated HOME/JCODE_HOME: one named test failed with ambiguous `openai` billing identity after a 4m32s compile and 0.27s test. Its proposed `ollama` identity proxy was rejected rather than accepted as a truthful fixture contract. Captain removed only that worker's changes and verified byte equality with the pre-edit snapshot. No production admission was weakened; J5 remains open.

Captain baseline task `400720rcbh` passed all 11 existing frozen-caller tests (27.81s test runtime, 69.2s command wall time), with local loopback fixtures and no paid inference. A fresh compact Sol/high packet owns only `attempt_caller.rs`, `attempt_caller_tests.rs` and `single_send_tests.rs`, bounded to two implementation iterations and 15 minutes active work. Its target is a baseline-failing, candidate-passing absolute-deadline regression. This packet has started, not completed. Previous long-context worker was retired.

## Accepted networking correction, 02:10 UTC

`952ab8928` uses one checked absolute deadline for guarded provider opening and subsequent stream consumption. It does not add a native CLI, change route selection, or establish a whole tool-using worker deadline. Unrepresentable durations preserve the prior effectively-unbounded behavior instead of introducing an overflow panic.

The permanent delayed-setup regression genuinely fails old production code: captain measured **1.879082917 seconds** for a one-second deadline, exactly one send and 500 micro-USD synthetic reserved exposure. The candidate passes the same test's 1.5-second upper bound, with one send and the same held exposure. Worker paired measurements were 1.855959959 seconds before and 1.002358292 after; these describe this deterministic fixture, not live-provider latency or financial savings.

Independent captain task `103636d48e` passed **13 caller tests**, **8 single-send tests** and **strict production-library Clippy**. The caller suite includes the overflow regression. Exact staged hashes matched the tested snapshot. Scoped formatting/whitespace passed. Native size, test-size and panic gates had no owned findings. The caller's swallowed-error finding is the unchanged pre-existing request serialization fallback, not a new deadline behavior. No baselines or warnings were suppressed.

Worker all-target Clippy remained blocked by the unchanged `openrouter_catalog_merge_tests.rs:129` environment lock across await. Broad workspace format/native budgets remain failed elsewhere. No whole-workspace green or runtime activation claim is made. Evidence: `~/.jcode/scratch/agents-captain-0127/deadline-captain-{hashes.json,baseline.log,caller.log,single.log,clippy.log}` and sibling `deadline-*-size.log`/budget logs.

This is the first accepted change in this economy cycle with a baseline-failing, candidate-passing time contract. The existing test command now automatically detects recurrence. Automatic model selection/promotion and generalized economic scoring remain unimplemented.
